# Prometheus scrape config. The Django app exposes /metrics via django-prometheus.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "sonani-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]
